services:
  swarm-engine:
    image: parity-swarm:latest
    container_name: swarm-engine
    env_file:
      - .env
    ports:
      - "3000:3000"
      - "5001:5001"
    restart: unless-stopped
    volumes:
      - ./backend/uploads:/app/backend/uploads
      
